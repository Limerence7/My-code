// 初始化头文件
#pragma once					// 防止被多次引用

#include <time.h>				// 时间函数的头文件
#include <conio.h>				// 控制台头文件
#include <algorithm>			// 算法函数头文件
#include <graphics.h>			// 图形库头文件
#include <math.h>				// 数学函数(三角函数)
#include <cstring>				// 字符头文件
#include <string>				// 字符串头文件
#include <Windows.h>			// Windows头文件
#include <mmsystem.h>
#pragma comment (lib, "winmm.lib")	// 音频头文件
using namespace std;